import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'stripe-ionic-react-sample',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
